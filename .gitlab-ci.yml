stages:
  - build
  - package
  - deploy

variables:
  SERVICE_NAME: iadp-front-web-demo

build:
  image: $REGISTRY_ADDRESS/devops/nodejs/build
  stage: build
  only:
    - demo
  script:
    - rm -f yarn.lock && yarn && yarn lint && yarn build
  artifacts:
    paths:
      - dist
      - Dockerfile
    expire_in: 2 hrs
  tags:
    - nodejs

package:
  image: $REGISTRY_ADDRESS/devops/docker/package
  stage: package
  variables:
    GIT_STRATEGY: none
  only:
    - demo
  script:
    - package
  tags:
    - docker

deploy:
  image: $REGISTRY_ADDRESS/devops/docker/deploy
  stage: deploy
  variables:
    GIT_STRATEGY: none
  dependencies: []
  only:
    - demo
  script:
    - deploy -service $SERVICE_NAME
  environment: aiads-dev
  tags:
    - docker

